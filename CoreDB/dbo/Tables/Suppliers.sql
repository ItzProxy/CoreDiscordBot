CREATE TABLE [dbo].[Suppliers] (
    [sid]     INT          NOT NULL,
    [sname]   VARCHAR (50) NULL,
    [address] VARCHAR (50) NULL,
    CONSTRAINT [PK_Table_1] PRIMARY KEY CLUSTERED ([sid] ASC)
);

